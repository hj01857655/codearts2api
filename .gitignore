auths/
data/
bin/
# 锚定到仓库根：面板产物 internal/server/dist/ 必须入库（发布不重建，
# 见 .github/workflows/release.yml），未锚定的 dist/ 会在任意深度命中。
/dist/
config.json
.env
*.key
*.pem
*.exe
*.backup2
*.backup
*.bak
*.backup2

# VS Code：只共享项目级调试/任务配置，个人设置（settings.json 等）不入库
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json

# IDE 本地产物
.arts/